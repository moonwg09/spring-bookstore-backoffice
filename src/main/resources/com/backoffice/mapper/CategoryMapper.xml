<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.backoffice.mapper.CategoryMapper">

    <select id="getList" resultType="com.backoffice.model.CategoryVO">
        SELECT * FROM Category 
        ORDER BY category_id ASC
    </select>

    <insert id="insert">
        INSERT INTO Category (parent_id, name)
        VALUES (#{parent_id, jdbcType=NUMERIC}, #{name})
    </insert>

    <select id="read" resultType="com.backoffice.model.CategoryVO">
        SELECT * FROM Category 
        WHERE category_id = #{category_id}
    </select>

    <update id="update">
        UPDATE Category
        SET parent_id = #{parent_id, jdbcType=NUMERIC}, 
            name = #{name}
        WHERE category_id = #{category_id}
    </update>

    <delete id="delete">
        DELETE FROM Category 
        WHERE category_id = #{category_id}
    </delete>

</mapper>